# Neon PostgreSQL — copy to .env and set your connection string, then run:
#   npx prisma migrate dev --name init
#   npx prisma db seed
#   npx prisma studio
# DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/DATABASE?sslmode=require"
DATABASE_URL=

# ENT Reporte (usado por portal-saas): provisionar admin al activar suscripción ENT.
# Configurar en portal-saas/.env.local y en Vercel (secrets). Ver DEPLOYMENT.md.
# ENT_REPORTE_API_URL=https://<url-de-la-cms-api>
# ENT_REPORTE_API_TOKEN=
