# Neon PostgreSQL — copy to .env and set your connection string, then run:
#   npx prisma migrate dev --name init
#   npx prisma db seed
#   npx prisma studio
# DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/DATABASE?sslmode=require"
DATABASE_URL=
